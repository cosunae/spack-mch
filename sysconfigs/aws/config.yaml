config:
  install_tree: /scratch/$user/spack/install/aws
  template_dirs:
    - $spack/templates
  module_roots:
    tcl:  /scratch/$user/spack/modules/ #to be changed to ?
  build_stage:
    - /scratch/$user/spack/spack-stages/aws
  install_path_scheme: '{name}/{version}/{compiler.name}/{hash}'
  source_cache: ~/.spack/source_cache
